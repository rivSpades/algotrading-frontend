/**
 * Dashboard Page (merged)
 *
 * Homepage is the live trading command center.
 */

import LiveDashboard from './LiveDashboard';

export default function Dashboard() {
  return <LiveDashboard />;
}

